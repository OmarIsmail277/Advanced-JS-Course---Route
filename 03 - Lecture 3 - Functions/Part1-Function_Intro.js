// Functions is a "block of code" designed to perform a particular task.
// It is executed when "something" invokes it (calls it).

// But, in JS it's bigger than that. If you understood Functions right, we can say that you understood 1/3 of JS. So, let's dive in!

// Because in JS, Functions is not only that you are calling a block of code, or a way to organize code.

/*
Functions do more than that:

1- Creates execution context. ( we will dive deeper in that when talking about how the browser understands JS).
2- Closure ( we will understand it today insha'Allah, it's a very important concept in JS, and it's one of the most powerful features of JS).
3- Scope Creator  (like the scopes of the variables created inside the function, and how they are not accessible outside the function).
4- Object ( can return an object from my function, like the constructor function, and we can use that object in our code).
4- Value (can return certain values from my function, and we can use that value in our code).
5- Class behavior ( we can use functions to create classes in JS).


*/
