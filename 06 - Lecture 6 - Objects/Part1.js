// =========================
// Intro to JavaScript Objects
// =========================

/*
Objects are one of the most important concepts in JavaScript.

In fact, JavaScript is often described as an object-based language,
and many of its unique behaviors revolve around how objects work.

In this section, we'll go beyond the typical object definition:

const user = {
  name: "Omar",
  age: 28
};

Instead, we'll ask deeper questions:

- What is an object really?
- Are there different ways to create objects?
- What does an object actually consist of internally?
- How are keys stored and accessed?
- Why does the order of keys sometimes change when logging an object?
- What does "copy by reference" really mean?
- What is the difference between shallow copy and deep copy?
- How are built-in properties different from properties we create ourselves?
- How does the prototype chain affect object behavior?
- What interview questions expose a deep understanding of objects?

We'll also explore common object-related APIs and operators:

- hasOwnProperty()
- Object.hasOwn()
- in operator
- Object.keys()
- Object.values()
- Object.entries()

By the end of this section, you'll understand objects not just as
"key-value pairs", but as one of the core building blocks of JavaScript.

Let's go deep into JavaScript Objects! 💪
*/
